# Data types Challenge
# Get a two digits input from a user and make those two digits be added together mathematically
two_digits = input("Type a two digits number: ")

first_digit = int(two_digits[0])
second_digit = int(two_digits[1])

add_two_digits = first_digit + second_digit
print(add_two_digits)
